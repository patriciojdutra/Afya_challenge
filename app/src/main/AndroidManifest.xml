<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.dutra.patricio.tvmaze">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.TvMaze">
        <activity
            android:name=".ui.peopledetails.PeopleDetailsActivity"
            android:parentActivityName=".ui.people.PeopleListActivity">
        </activity>
        <activity
            android:name=".ui.people.PeopleListActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".ui.movie.MovieListActivity">
        </activity>
        <activity
            android:name=".ui.episode.EpisodeActivity"
            android:parentActivityName=".ui.moviedetails.MovieDetailsActivity">
        </activity>
        <activity
            android:name=".ui.moviedetails.MovieDetailsActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".ui.movie.MovieListActivity">
        </activity>
        <activity
            android:name=".ui.movie.MovieListActivity"
            android:launchMode="singleTop" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>